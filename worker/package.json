{
  "name": "stage0-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute stage0-catalog --remote --file schema.sql",
    "tail": "wrangler tail",
    "test:security": "node test/security.mjs",
    "preview": "wrangler d1 execute stage0-catalog --local --file schema.sql && node test/seed.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "wrangler": "^3.99.0"
  }
}
